{
  "name": "cursor-study-blog",
  "private": true,
  "scripts": {
    "prepare": "husky",
    "precommit": "npm run lint --prefix client && npm run test:run --prefix client"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  }
}
